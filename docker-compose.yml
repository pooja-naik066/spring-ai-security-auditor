services:
  postgres:
    image: pgvector/pgvector:pg17
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_DB=auditor_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
